STEP 1 → Create project structure
STEP 2 → Setup Docker (Postgres + pgvector)
STEP 3 → Setup DB schema
STEP 4 → Setup DB connection in Python
STEP 5 → Build ingestion pipeline
STEP 6 → Test embeddings stored in DB
STEP 7 → Build vector retrieval
STEP 8 → Connect LLM
STEP 9 → Create FastAPI endpoint
STEP 10 → Create Streamlit UI